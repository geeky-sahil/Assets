#include<stdio.h>
#include<conio.h>
void main()
{
    int a,b,sum;    /*a,b are two numbers whose sum is to be stored in sum*/
    printf("Enter the values of a.b:");
    scanf("%d%d",&a,&b);
    sum=a+b;
    prinf("%d",sum);
    getch();
}